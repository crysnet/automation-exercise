TEST_BASE_URL= # Required
# HEADLESS= # Optional: true/false (default: true) - whether to run tests in headless mode
# WORKERS= # Optional: number of parallel workers (default: 1 number)
# RETRIES= # Optional: number of retries for failed tests (default: 0)